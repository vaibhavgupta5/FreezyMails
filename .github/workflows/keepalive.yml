name: Keep Render Worker Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch: 
  push:
    branches: [ main ] 

jobs:
  ping-render:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Worker URL
        run: curl -s -f https://freezymails.onrender.com/ || echo "Ping failed, but workflow will continue"
